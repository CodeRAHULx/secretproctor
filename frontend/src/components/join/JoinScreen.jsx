export function JoinScreen({ meeting }) {
  const { form, setForm, error, joining, identity, join, googleSignIn } = meeting;
  const change = event => setForm(old => ({ ...old, [event.target.name]: event.target.value }));
  return <main className="join-shell"><section className="join-panel"><div className="brand intro"><b>S</b> SecureMeet</div><p className="eyebrow">SECURE INTERVIEW WORKSPACE</p><h1>Enter your protected session.</h1><p className="lead">A calm, auditable meeting space with continuous integrity monitoring.</p><button className={`google ${identity ? 'connected' : ''}`} type="button" onClick={googleSignIn}>{identity ? 'Google account connected' : 'G  Continue with Google'}</button><p className="auth-state">{identity ? `Signed in as ${identity.email}` : 'Google sign-in is optional for this session.'}</p><div className="divider">or use meeting access</div><form onSubmit={join}><label>Meeting code<input name="sessionId" value={form.sessionId} onChange={change} required /></label><label>Your name<input name="participantName" value={form.participantName} onChange={change} required /></label><div className="form-row"><label>Role<select name="role" value={form.role} onChange={change}><option value="candidate">Candidate</option><option value="interviewer">Interviewer</option></select></label><label>Passcode<input name="passcode" type="password" value={form.passcode} onChange={change} required /></label></div>{error && <p className="form-error" role="alert">{error}</p>}<button className="primary" disabled={joining}>{joining ? 'Verifying access...' : 'Join secure session'}</button></form><p className="fineprint">By joining, you acknowledge this session’s integrity and recording policies.</p></section><aside className="join-aside"><span className="orb orb-one" /><span className="orb orb-two" /><div><p className="eyebrow">BUILT FOR FOCUS</p><h2>Security that stays in the background until it matters.</h2><ul><li>Native display-affinity monitoring</li><li>Real-time integrity signals</li><li>Exportable audit trail</li></ul></div></aside></main>;
}
